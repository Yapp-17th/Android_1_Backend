server:
  port: 8082
spring:
  application:
    name: domain
eureka:
  instance:
    prefer-ip-address: true # 서비스간 통신 시 hostname 보다 ip 를 우선 사용 - 개발모드
  client:
    service-url:
      defaultZone: http://172.17.0.1:8761/eureka # 클라이언트 서버가 등록되는 경로
