spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
    show-sql: true
JWT:
  SECRET:
    KEY: TestJwtSecretKey
  TOKEN:
    VALIDITY:
      ACCESS: 600
      REFRESH: 604800